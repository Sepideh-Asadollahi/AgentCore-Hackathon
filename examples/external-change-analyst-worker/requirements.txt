# AgentCore integrator example worker
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.8.0
langgraph>=0.2.0
langchain-core>=0.3.0

# Install SDK from monorepo (see README):
# pip install -e ../../sdk/python  # not a package — use PYTHONPATH instead
