# Interfaces

Inbound FastAPI adapter and explicit request/response schemas for managed agents, lifecycle/heartbeat, agent tickets, society runs, approvals, messages, and evaluation. Transport validates shape and scope, then delegates to application use cases.
