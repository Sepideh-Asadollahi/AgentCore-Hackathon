# Copy to push-github-hackathon.local.env (gitignored).
# Remote is allowlisted in the script — only AgentCore-Hackathon (HTTPS or SSH below).
# Do NOT put API keys here — use hackathon/.env for QWEN_API_KEY.

# Optional: pick SSH instead of default HTTPS (must match allowlist exactly).
# GITHUB_REMOTE_URL="git@github.com:Sepideh-Asadollahi/AgentCore-Hackathon.git"

GITHUB_BRANCH="main"
GITHUB_COMMIT_MESSAGE="chore: sync Change Society hackathon submission"
