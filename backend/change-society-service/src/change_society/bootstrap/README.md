# Bootstrap

Composition root and typed environment configuration. This is the only layer that loads managed-agent profiles and chooses concrete model, run store, control-plane store, agent adapters, clock, ID, and transport adapters.
