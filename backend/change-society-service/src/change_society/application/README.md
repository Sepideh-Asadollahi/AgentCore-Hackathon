# Application

Framework-independent `AgentControlPlane`, capability router, society orchestration, approval, context retrieval, persistence/model/adapter ports, and evaluation. Application code controls tickets and policy but never constructs or executes a vendor runtime directly.
