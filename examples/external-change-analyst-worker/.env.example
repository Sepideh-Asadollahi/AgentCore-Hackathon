# Worker
AGENTCORE_WEBHOOK_SHARED_SECRET=integrator-demo-secret-change-me
WORKER_HOST=0.0.0.0
WORKER_PORT=32510

# Live integrator (all roles via Qwen + LangGraph for change_analyst)
WORKER_LIVE_MODE=1
WORKER_RUNTIME_NAME=langgraph-qwen-society-worker
QWEN_API_KEY=
QWEN_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-plus
QWEN_TIMEOUT_SECONDS=120

# Offline / unit tests only
# WORKER_LIVE_MODE=0
# WORKER_RUNTIME_NAME=langgraph-change-analyst

# Control plane (same secret + live-all registry):
# CHANGE_SOCIETY_WEBHOOK_AGENT_SECRET=integrator-demo-secret-change-me
# CHANGE_SOCIETY_MANAGED_AGENTS_CONFIG=hackathon/backend/change-society-service/config/managed-agents.integrator-live-all.example.json
