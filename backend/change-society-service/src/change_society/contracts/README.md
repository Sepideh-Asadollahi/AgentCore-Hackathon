# Contracts

Versioned Pydantic wire contracts for Universal Agent JSON and model-role outputs plus provider-neutral `AgentExecutionRequest` and `AgentExecutionResult` adapter contracts. Contracts contain no workflow orchestration.
